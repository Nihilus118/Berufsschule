#include <iostream>

using namespace std;

double lvp(double lep, double lr, double lskonto, double bezkosten, double handkosten, double gewinn, double kundenskonto, double kundenrabatt) {
    double zieleinkp, bareinkaufp, bezugspreis, selbstkosten, bvk, zvp, lvp;

    zieleinkp = lep + lep * lr;
}

int main() {
    return 0;
}
